import React, { Component, Fragment } from 'react';

export default class Pagination extends Component {
  constructor(props) {
    super(props);
    const { bootcamps, pageLimit = 10 } = props;
  }
  render() {
    return <div></div>;
  }
}
